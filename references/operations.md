# 运行层 · 推进、验证、收口

> 从 `SKILL.md` 分出来的按需内容。做研发 / 跑实验 / 决定停不停的时候翻这一份。

---

## 研发 / 实验运行原则 · 让设备一直跑

*五步管"怎么决策",这一条管"怎么保持推进"。做研发和实验的时候用。*

**核心:永远有东西在跑。**

- **设备 / 算力不空转。** 训练尽量不要停,实验台尽量不要空。让下一组实验在上一组还没出结果时就排上 —— **排队等结论是最大的浪费。**
- **一个方向不行 → 换方向,不要停。** 卡住不是停工的理由,是换路的信号。
- **卡住时三选一,但别选"等着":**

| 出路 | 做什么 |
|---|---|
| **换方向** | 试另一条路径 / 另一组参数 / 另一个假设 |
| **定位问题** | 分层排查:到底是哪一环错了?找到真正那一层 |
| **修,再试** | 修完立刻重跑,别让"修"变成新的等待 |

- **失败必须变成下一轮的输入。** 一次实验跑完,必须产出"下一次跑什么"。跑完没有下一轮 = 白跑。
- **并行优先,串行是最后手段。** 能同时跑的方向就同时跑,顺序试是没办法才用。

**和"别在坑里加速"不冲突:**
- "别在坑里加速" = 别在**同一条错路**上更快
- 这条 = 别让**整体的实验吞吐**掉下来
- 判定一个方向是坑 → **关掉它,把资源移到别的方向**,不是停下来

**反直觉的点:** "停下来想清楚"常常是拖延的高级形式。正确姿势是**让实验继续跑着,同时想** —— 想和跑并行,不是先后。真的需要停,也只能局部停,不能全局停。

---

## RSI 闭环 · 让改进本身可验证

*五步管"怎么决策",运行原则管"怎么保持推进",这一节管**"怎么确认改进是真的"**。*

**闭环:**

```
提议 → 评估(冻结评估器) → 晋升门 → 保留 / 回滚 → 记录血缘 → 下一代
```

**七条硬规则:**

1. **评估器不可改。** 改进者不能碰判分规则 —— 测试、门禁、反作弊、循环编排本身,全部冻结。**改得了裁判,分数就没有意义。**
2. **留出集必须封存。** 搜索过程看不到最终测试。看得见的测试,迟早会被拟合。
3. **没进晋升门,不算改进。** "我觉得变好了"不是证据。只有显式 gate 通过才算数,而且只有它能接受候选。
4. **被拒的必须回滚。** 干净退回上一版,不留半成品 —— 半成品会让下一轮的基线不可信。
5. **每代记血缘。** 谁改了哪、依据什么、结果如何,可追溯、可复现。没有血缘 = 复现不了 = 等于没发生。
6. **失败要留下残渣,喂给下一轮。** 失败不是噪声,是下一轮的课程来源。拒绝理由要结构化留存,不是丢掉。
7. **建与审分离。** 提议的人不给自己打分。条件允许就上红队对抗 —— 让另一双眼睛专门找茬。

**判据:这一轮的改进,别人能复现吗?**
- 能 → 它是真的
- 不能 → 它只是你的感觉

**和前面几节的关系(这几层是闭合的):**
- 第 -1 步"假设可否证伪" → 冻结评估器就是这个判据的落地
- 运行原则"失败变成下一轮输入" → 就是第 6 条失败残渣
- 第 2 步"删到要加回 10%" → 留出集 + 回滚就是那个安全阀

---

## 什么时候停

*前面全在说"别停"。这一节说**什么时候必须停** —— 判据只有一条:**任务目标**。不看进度条,不看心情,不看累不累。*

**三个停止条件(全部锚在目标上):**

| # | 条件 | 动作 |
|---|---|---|
| 1 | **目标达成** | **停**,收工。达成标准在第 0 步就写好了(可测量),不是事后说"差不多了" |
| 2 | **目标被证伪 / 不再成立** | **停**这个方向,回第 0 步重立目标。**这不是失败,是目标层的结论** |
| 3 | **代价超过目标价值** | **停**,或降级目标。靠预算闸判定(见下) |

**明确不算停止理由的:**

- "删得差不多了" —— 不是理由。判据是"该加回 10% 了"。
- "跑腻了 / 没灵感 / 团队累了" —— 不是理由。这是**换方向**,不是停。
- "这个方向失败了" —— **不是停止,是换路**。只有**目标本身**出问题才停。

**刹车 vs 换路(最容易搞混):**

| 情况 | 动作 | 层 |
|---|---|---|
| 一个方向不行 | **换方向** | 运行原则 |
| 目标不可能 / 没意义 | **停**,回第 0 步重立目标 | 目标层 |
| 代价超价值 | **停**,或降级目标 | 目标层 |
| 目标达成 | **停**,收工 | 目标层 |

**预算闸(让"代价超价值"可判定):**

第 0 步钉死的时间盒 / 成本上限 / 轮次上限,到线就**强制复盘**,三选一必须明确选:

```
继续  /  换路  /  停
```

**默认是停,不是继续。** 想继续,要给出理由 —— 而不是默认续期。

**删除的刹车:** 见 `SKILL.md` 第 2 步的可逆性分级。不可逆的东西不适用"疯狂删除"。

---

## 记录格式(一句话一步)

```
[-1 参照系] 约束是物理还是约定:___   假设可否证伪:___
[0 目标]    往哪造:___  指标:___  预算(时间/成本/轮次):___
[1 质疑]    要求来源:___             是否成立:___
[2 删除]    删掉了:___               加回了:___ (≥10%?)  可逆?___
[3 简化]    简化点:___
[4 加速]    循环时间:___ → ___
[5 自动化]  是否自动化:___           理由:___
[RSI]       评估器冻结?___ 进晋升门?___ 血缘:___
[停]        目标达成?___ 目标还成立?___ 到预算线?___ → 继续/换路/停:___
[下一轮]    这次跑完,下一步跑什么:___
```
